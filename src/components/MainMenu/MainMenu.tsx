import React from 'react';
import Logo from '../Logo';

const MenuElement = () => {
    
}

const MainMenu = () => (
    <React.Fragment>
        <Logo alt='Back to homepage' />
        <menu>
            <ul>

            </ul>
        </menu>
    </React.Fragment>
)

export default MainMenu;